name = "apilab-mcp-api"
main = "index.ts"
compatibility_date = "2024-11-17"

# Cloudflare Workers configuration
workers_dev = true

# Node.js compatibility for e2b package
compatibility_flags = ["nodejs_compat"]

# Environment variables (optional - API key dari frontend)
# [vars]
# E2B_API_KEY = "" # Optional - bisa dari frontend request

# Secrets (gunakan: wrangler secret put E2B_API_KEY)
# Tapi untuk hackathon, API key dari frontend lebih mudah
